- flash.each do |key, notice|
  .alert.alert-dismissible{role: "alert", class: flash_alert_class(key)}
    %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
      %span{"aria-hidden" => "true"} ×
    %strong #{key.to_s.titleize}:
    %span{class: key}= notice
